---
title: Marketing
description: Layout primitives for marketing pages — hero, sections, rivers, pillars.
---

import Playground from "@/playground/Playground.astro";
import MARKETING from "@/playground/files/ui-marketing.tsx?raw";

A CSS-only system for marketing pages. Class + data-attribute API, no JS. A page is a stack of `.page-section`s; each section uses `.page-container` to gutter and cap its content; rivers arrange a text + visual pair side-by-side; pillars and testimonials are vertical mini-cards; `.cta-banner` is a centered call-to-action panel; `.statistic` surfaces large display numbers.

<Playground
  provider={{ files: { "/main.tsx": { code: MARKETING, active: true } } }}
  preview={{}}
/>

## Install

```ts
import "elements-kit/ui/styles/theme.css";       // --color-background, --color-overlay, transitively --base-color-N
import "elements-kit/ui/styles/scaling.css";     // --scaling factor (data-scaling="xs..xl")
import "elements-kit/ui/styles/typography.css";  // --font-size-N, --line-height-N, --font-weight-*
import "elements-kit/ui/styles/radius.css";      // --radius-N (data-radius="none..pill")
import "elements-kit/ui/marketing/marketing.css";
```

The whole file is wrapped in `@layer marketing`, so any unlayered host CSS wins on collision — no specificity wars. Backgrounds use semantic tokens from theme.css (`--color-background`, `--color-overlay`) — they flip with `.dark` / `.light` on a parent. Text and borders use the base neutral palette (`--base-color-N`, `--base-color-aN`) defined in `palette/base.css` (defaults to gray; pick a different neutral with `data-gray-color="slate|sage|sand|mauve|olive"`). Typography scales follow kit tokens; display headings (≥48px) multiply by `--scaling` so `data-scaling="lg"` resizes them. Radii follow `--radius-N` so `data-radius="pill"` rounds marketing borders. The pillar icon follows `--color-9` — set `data-color="X"` on an ancestor (e.g. `data-color="mint"`) to pick the accent palette.

Host-page CSS variables expected (sensible fallbacks ship in `.page-container`):

| Variable | Purpose | Fallback |
| --- | --- | --- |
| `--page-padding-left` | left gutter for `.page-container` | `1rem` |
| `--page-padding-right` | right gutter for `.page-container` | `1rem` |
| `--page-max-width` | max width for `.page-container` | `72rem` |
| `--font-heading` | heading font family | inherit |

## Composition

```html
<section class="page-section">
  <div class="page-container">
    <header class="section-header">
      <h2 class="section-heading section-heading-2">Title</h2>
      <p class="section-paragraph">Lede paragraph.</p>
    </header>

    <div class="river" data-align="start">
      <div class="river-content">
        <h3 class="section-heading section-heading-3">Feature</h3>
        <p class="section-paragraph">Detail copy.</p>
        <a href="/learn">Learn more</a>
      </div>
      <div class="river-visual" data-border>
        <img src="/feature.webp" alt="" />
      </div>
    </div>
  </div>
</section>
```

The standard nesting is **page-section → page-container → (section-header) + (river+ | pillar+ | testimonial+ | cta-banner | statistic+)**. Heros sit at the top of the page and skip the wrapping `.page-section`.

## Primitives

### Hero

Top-of-page block. Flex column container.

```html
<section class="hero" data-align="center">
  <h1 class="section-heading section-heading-1">Headline</h1>
  <p class="section-paragraph">Subhead</p>
</section>
```

| Attribute | Values |
| --- | --- |
| `data-align` | `start` (default), `center` |

:::tip[Do]
Keep heading ≤ 2 lines on desktop. Paragraph ≤ 75ch.
:::

:::caution[Don't]
Stack three CTAs side-by-side. Pick one primary plus an optional secondary link.
:::

### Page section

A page-level chunk. Themed background (`--color-background`), vertical padding.

```html
<section class="page-section">
  <div class="page-container">…</div>
</section>
```

No data attributes.

:::caution[Don't]
Nest one `.page-section` inside another. Rivers and headers go directly inside `.page-container`.
:::

### Page bounds

Pure layout container. Applies `--page-padding-*` gutters and `--page-max-width` cap, centered.

```html
<div class="page-container">…</div>
```

No data attributes. Place directly inside `.page-section`.

:::tip[Do]
Let the var-driven system handle width and gutters.
:::

:::caution[Don't]
Apply your own `max-width` or horizontal padding on children. Override the page CSS variables instead.
:::

### Section header

Heading + paragraph + optional CTA cluster. Sits above rivers in a section.

```html
<header class="section-header" data-align="center">
  <h2 class="section-heading section-heading-2">Title</h2>
  <p class="section-paragraph">Lede.</p>
  <a href="/cta">Action</a>
</header>
```

| Attribute | Values |
| --- | --- |
| `data-align` | `start` (default), `center` |

:::tip[Do]
One heading + one paragraph + optional CTA.
:::

:::caution[Don't]
Put marketing copy paragraphs inside the header. That's what River content is for.
:::

### Section heading

Four sizes, paired with a base `.section-heading` class.

```html
<h1 class="section-heading section-heading-1">Display</h1>
<h2 class="section-heading section-heading-2">Section</h2>
<h3 class="section-heading section-heading-3">Sub-section</h3>
<h4 class="section-heading section-heading-4">Eyebrow</h4>
```

| Class | Use for |
| --- | --- |
| `.section-heading-1` | hero / display headline (48px × scaling → 60px at lg via `--font-size-9`) |
| `.section-heading-2` | section header (35px via `--font-size-8` → 48px × scaling at lg) |
| `.section-heading-3` | river / sub-section (30px × scaling → 35px via `--font-size-8` at lg) |
| `.section-heading-4` | minor labels (20px via `--font-size-5`) |

Pick the level that matches the document outline (`<h1>` once per page), not the visual size. Pair `.section-heading-1` with `<h2>` if the page already has an `<h1>`.

### Section paragraph

Body text token. Preserves line breaks. Inline `<em>` highlights in the brightest neutral.

```html
<p class="section-paragraph">
  Default copy color is <em>--base-color-11</em>. Emphasized inline runs are <em>--base-color-12</em>.
</p>
```

### River

Two-column grid: text + visual side-by-side. Alternate `data-align` between adjacent rivers for a flowing rhythm.

```html
<div class="river" data-align="start">
  <div class="river-content">…</div>
  <div class="river-visual">…</div>
</div>
```

| Attribute | Values |
| --- | --- |
| `data-align` | `start` (default — text left), `end` (text right), `center` (stacked, centered), `breakout` (custom grid for headline + trailing element) |
| `data-large-visual` | modifier — visual takes 7/12, content 5/12 (only with `start` / `end`) |

:::tip[Do]
Alternate `data-align="start"` and `"end"` between adjacent rivers for visual rhythm.
:::

:::caution[Don't]
Mix `data-align="center"` with `data-large-visual`. The 5/7 grid only applies to `start` / `end`.
:::

:::tip[Do]
Keep visuals roughly the same aspect ratio across consecutive rivers.
:::

:::caution[Don't]
Leave `.river-visual` empty as a spacer. Use a regular `<div>` if you need negative space.
:::

### River content / visual

```html
<div class="river-content">
  <h3 class="section-heading section-heading-3">Heading</h3>
  <p class="section-paragraph">Body.</p>
  <a href="/learn">Learn more</a>
</div>

<div class="river-visual" data-border>
  <img src="/feature.webp" alt="" />
</div>
```

| Element | Attribute | Values |
| --- | --- | --- |
| `.river-visual` | `data-border` | presence — adds a 1px border + rounded corners |

### Pillar

Vertical mini-card for a feature, benefit, or stat. Anatomy: icon → heading → paragraph → link.

```html
<div class="pillar" data-border="true">
  <svg><!-- icon --></svg>
  <h3 data-heading>Heading</h3>
  <p data-paragraph>Description.</p>
  <a href="/learn">Learn more</a>
</div>
```

| Attribute | Values |
| --- | --- |
| `data-border` | `true` — card treatment (border + padding) |
| `data-align` | `start` (default), `center` |

The icon uses `--accent-9` so a single Radix accent change re-themes every pillar at once.

:::tip[Do]
When using icons, add them to every pillar in the group.
:::

:::caution[Don't]
Mix icons-with-pillars and no-icons-with-pillars in the same row.
:::

:::tip[Do]
Keep heading character counts comparable across a pillar group for a visually balanced layout.
:::

:::caution[Don't]
Let one description run 4× the length of its siblings.
:::

:::tip[Do]
Use `data-border` for elevated card-style pillars when the group sits on a colored surface.
:::

:::caution[Don't]
Combine `data-border` with `data-align="center"`. The border treatment expects start-aligned content.
:::

### Testimonial

Quote card. Anatomy: oversized opening quote glyph → blockquote body → figcaption (cite + title).

```html
<figure class="testimonial">
  <blockquote>
    The kit <em>cut our design-to-ship loop in half</em>.
  </blockquote>
  <figcaption>
    <cite>Jane Doe</cite>
    <span data-title>Staff Engineer, Acme Co</span>
  </figcaption>
</figure>
```

| Element | Notes |
| --- | --- |
| `> blockquote` | Pulls quote glyphs via `::before` / `::after`. `<em>` inside renders in `--base-color-12`. |
| `> figcaption > cite` | Name. Always non-italic. |
| `> figcaption > [data-title]` | Role / company line. |

The quote glyph color follows `--color-9` — set `data-color="X"` on an ancestor to re-theme.

:::tip[Do]
Keep quote body under ~3 lines on desktop. Use `<em>` to highlight the punchy phrase.
:::

:::caution[Don't]
Stack multiple paragraphs in the blockquote. One thought, one card.
:::

### CTA banner

Bordered call-to-action panel. Centered by default; flips to start-aligned with `data-align="start"`.

```html
<div class="cta-banner">
  <h2 class="section-heading section-heading-2">Ship faster with composable primitives.</h2>
  <p class="section-paragraph">
    Class + data-attribute API. No runtime, no theming surprises.
  </p>
  <div data-cta>
    <a href="/start">Get started</a>
  </div>
</div>
```

| Attribute | Values |
| --- | --- |
| `data-align` | `center` (default), `start` |

Wrap CTAs in a `<div data-cta>` — that's the row that picks up the flex-wrap + gap.

:::tip[Do]
Use sparingly. One CTA banner near the bottom of the page, not every section.
:::

:::caution[Don't]
Add a background image inside the primitive. Decorate from the caller — the kit handles layout only.
:::

### Statistic

Vertical stack: small leading label → large display number → body description.

```html
<div class="statistic">
  <p data-leading>Up to</p>
  <p data-heading>75%</p>
  <p data-description>faster builds across the org.</p>
</div>
```

| Element | Notes |
| --- | --- |
| `[data-leading]` | Small label above the number. Optional. |
| `[data-heading]` | Display number (48px × scaling, up to `--font-size-9` at lg). |
| `[data-description]` | Body sentence below. |

| Attribute | Values |
| --- | --- |
| `data-size` | `1` (small), default (medium), `3` (large display) |
| `data-align` | `start` (default), `center` |

Sizes mirror `.x-button` — `data-size="1"` for inline / dense rows, no attribute for the standard section-level stat, `data-size="3"` for hero-tier display numbers (weight 550, tight tracking, scales via `--scaling`).

To align multiple statistics in a row, wrap them in your own grid — there's no `.statistic-row` primitive:

```html
<div style="display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 2rem">
  <div class="statistic">…</div>
  <div class="statistic">…</div>
  <div class="statistic">…</div>
</div>
```

:::tip[Do]
Keep the numbers comparable in magnitude (all percentages, or all counts) within a row.
:::

:::caution[Don't]
Use `[data-heading]` for long phrases. It's a display slot for a number or 1–2 words.
:::

## Theming

The marketing layer reads four token groups:

- `--color-background` (page bg) and `--color-overlay` (stacked surface overlay) — semantic role tokens from [theme.css](/ui/styles). Both flip automatically with `.dark` / `.light` on a parent.
- `--base-color-1..12` and `--base-color-a1..12` — neutrals used for text + borders. Provided by `palette/gray.css` (or by Radix `<Theme>` if you're using Radix).
- `--accent-1..12` — brand accent. Resolved by the host. Marketing only reads `--accent-9` (pillar icon).
- Page CSS vars: `--page-padding-left`, `--page-padding-right`, `--page-max-width`, `--font-heading`.

Switching `.dark` ↔ `.light` on a parent re-themes the surface and text. Switching the host `accentColor` re-themes the icon. No `data-color` setup required.
